[tool.pyright]
# off , basic , standard , strict
typeCheckingMode = "standard"

exclude = [
    # Default
    "**/node_modules",
    "**/__pycache__",
    "**/.*",

    "venv",
    "tests",
]

ignore = [
    # Runtime, python libs
    "**/dist/**",
    "**/Lib/**",

    "**/launcher/*.py",
    "**/launcher/pages",
    "**/launcher/gui_parts",
    "**/launcher/components",
]

[tool.ruff]
extend-exclude = ["tests"]

lint.extend-select = ["I", "SIM", "B"]
